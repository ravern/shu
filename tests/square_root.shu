use std::math

pub fn square_root(n) {
  do_square_root(n, n)
}

fn do_square_root(n, x) {
  let root = 0.5 * (x + n / x)
  if math::abs(root - x) > 0.01 {
    root
  } else {
    do_square_root(n, root)
  }
}