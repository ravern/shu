mod parser {
  fn new() {
    Parser {
      start: 0,
      end: 0,
    }
  }

  fn parse(parser) {
    // Do some stuff here.
  }
}

fn main() {
  let parser = parser::new();
  parser::parse(parser);
  parser.parse().parse()
}

