#[derive(Debug, PartialEq)]
pub enum Token {
  Int(i64),
  Float(f64),
  Bool(bool),
  String(String),
  Add,
  Sub,
  Mul,
  Rem,
  Dot,
  And,
  Or,
  Gt,
  Gte,
  Lt,
  Lte,
  Eq,
  Not,
  Assign,
  LParen,
  RParen,
  LBrace,
  RBrace,
  LBracket,
  RBracket,
  Comment(String),
}
